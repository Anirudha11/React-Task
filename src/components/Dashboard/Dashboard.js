import React from 'react';
// import DashboardData from '../../store/json/dashboard.json';

const dashboard = (props) => {
    
    return (
        <React.Fragment>
        <h1>Dashboard UI!</h1>
        {/* {DashboardData.user.map(user=><h1>{user.name}</h1>)} */}
        </React.Fragment>
        
    )
}

export default dashboard;